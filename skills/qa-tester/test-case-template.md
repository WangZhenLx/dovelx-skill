# 测试用例模板

> 文件保存路径：`.claude/doc/<功能名>/test-cases-<YYYY-MM-DD>-v1.md`

---

## 基本信息

| 项目 | 内容 |
|------|------|
| **功能模块** | （如：用户认证 / 订单管理 / 支付流程） |
| **测试版本** | v1.0.0 |
| **编写人** | QA Agent |
| **编写日期** | YYYY-MM-DD |
| **测试环境** | dev / staging / prod |
| **关联需求** | （需求文档链接或编号） |

---

## 测试范围

**In Scope（测试范围内）**
- （列举本次要测试的功能点）

**Out of Scope（测试范围外）**
- （明确不在本次测试范围内的内容，避免遗漏引起误解）

---

## 测试用例列表

### 模块一：[模块名称]

#### 功能测试

| 用例 ID | 测试标题 | 优先级 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 实际结果 | 状态 | 备注 |
|---------|---------|--------|---------|---------|---------|---------|---------|------|------|
| TC-XXX-001 | 正常创建订单（完整参数） | P0 | 用户已登录，商品库存 > 0 | `{ "productId": "p001", "quantity": 2, "address": "北京市..." }` | 1. 发送 POST /orders 请求 2. 携带完整参数 | 返回 201，body 含 `orderId`，订单状态为 `PENDING`，库存减少 2 | - | ⏭️ | |
| TC-XXX-002 | 未登录用户无法下单 | P0 | 用户未登录（无 Token） | 同上 | 1. 不携带 Authorization header 2. 发送 POST /orders | 返回 401，`{ "code": "UNAUTHORIZED" }` | - | ⏭️ | |
| TC-XXX-003 | 下单后库存不足时拒绝 | P1 | 商品库存 = 1 | `{ "quantity": 2 }` | 1. 下单数量超过库存 | 返回 400，`{ "code": "INSUFFICIENT_STOCK" }` | - | ⏭️ | |

#### 业务闭环测试

| 用例 ID | 测试标题 | 优先级 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 实际结果 | 状态 | 备注 |
|---------|---------|--------|---------|---------|---------|---------|---------|------|------|
| TC-XXX-010 | 完整订单生命周期（创建→支付→完成） | P0 | 用户已登录，商品有库存 | 标准下单参数 | 1. POST /orders（创建订单）2. POST /payments（支付）3. 系统回调支付成功 4. GET /orders/{id}（查询状态） | 最终订单状态为 `COMPLETED`，库存已扣减，流水记录存在 | - | ⏭️ | 端到端业务闭环 |
| TC-XXX-011 | 取消订单后库存回滚 | P1 | 已创建订单，未支付 | `{ "orderId": "xxx" }` | 1. POST /orders/{id}/cancel | 订单状态变为 `CANCELLED`，库存恢复到取消前 | - | ⏭️ | 验证数据回滚 |
| TC-XXX-012 | 重复提交下单请求（幂等性） | P1 | 用户已登录 | 携带相同 `requestId` 的两次请求 | 1. 发送第一次 POST /orders 2. 立即发送相同 requestId 的第二次请求 | 只创建一条订单，第二次返回第一次的结果 | - | ⏭️ | 防重复提交 |

#### 边界值测试

| 用例 ID | 测试标题 | 优先级 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 实际结果 | 状态 | 备注 |
|---------|---------|--------|---------|---------|---------|---------|---------|------|------|
| TC-XXX-020 | 购买数量 = 0 | P1 | 用户已登录 | `{ "quantity": 0 }` | POST /orders | 返回 400，提示数量必须大于 0 | - | ⏭️ | 最小值边界 |
| TC-XXX-021 | 购买数量 = 最大限制值 | P1 | 用户已登录，库存充足 | `{ "quantity": MAX_LIMIT }` | POST /orders | 下单成功，返回 201 | - | ⏭️ | 最大值边界 |
| TC-XXX-022 | 购买数量 = 最大限制值 + 1 | P1 | 用户已登录 | `{ "quantity": MAX_LIMIT + 1 }` | POST /orders | 返回 400，提示超过单次购买上限 | - | ⏭️ | 超出边界 |
| TC-XXX-023 | 商品 ID 为空字符串 | P1 | 用户已登录 | `{ "productId": "" }` | POST /orders | 返回 400，提示 productId 不能为空 | - | ⏭️ | 空字符串边界 |
| TC-XXX-024 | 商品 ID 包含特殊字符（SQL 注入尝试） | P0 | 用户已登录 | `{ "productId": "'; DROP TABLE orders;--" }` | POST /orders | 返回 400 或 404，数据库不受影响 | - | ⏭️ | 安全边界 |
| TC-XXX-025 | 地址字段超过最大长度 | P2 | 用户已登录 | address 字段超过 255 字符 | POST /orders | 返回 400，提示地址过长 | - | ⏭️ | 字段长度边界 |

#### 规范测试

| 用例 ID | 测试标题 | 优先级 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 实际结果 | 状态 | 备注 |
|---------|---------|--------|---------|---------|---------|---------|---------|------|------|
| TC-XXX-030 | 响应格式符合接口规范 | P1 | 正常下单成功 | 标准参数 | POST /orders 并检查响应体 | 响应包含规范中定义的所有字段（orderId、status、createdAt 等），字段名大小写完全一致 | - | ⏭️ | 对照接口文档 |
| TC-XXX-031 | 错误响应包含 code 和 message 字段 | P1 | 任意触发错误 | 无效参数 | POST /orders 传入错误参数 | `{ "code": "XXXX", "message": "..." }` 格式正确 | - | ⏭️ | 错误响应规范 |
| TC-XXX-032 | 时间字段格式为 ISO8601 | P2 | 正常下单 | 标准参数 | 检查响应中 createdAt 字段 | 格式为 `2024-01-01T12:00:00Z` | - | ⏭️ | 日期格式规范 |

#### 性能测试

| 用例 ID | 测试标题 | 优先级 | 场景设置 | 测试方法 | 预期结果 | 实际结果 | 状态 | 备注 |
|---------|---------|--------|---------|---------|---------|---------|------|------|
| TC-XXX-040 | 单次下单接口响应时间 | P1 | 单用户单次请求 | 发送 POST /orders，记录响应时间 | P95 < 500ms | - | ⏭️ | |
| TC-XXX-041 | 批量查询订单列表（1000 条） | P1 | 数据库已有 10 万条订单 | GET /orders?limit=1000 | 响应 < 1s，无超时 | - | ⏭️ | 检查是否有索引和分页 |
| TC-XXX-042 | 并发 100 用户同时下单 | P2 | 同一商品库存 = 200 | 并发 100 个请求 | 无超卖（最终库存 ≥ 100），无系统报错 | - | ⏭️ | 并发安全 |

---

## 测试数据准备

| 数据类型 | 说明 | 准备方式 |
|---------|------|---------|
| 测试用户账号 | 已注册、已实名、未实名各一个 | 脚本或手动创建 |
| 测试商品 | 有库存商品、库存为 0 商品、已下架商品 | 脚本注入 |
| 历史订单数据 | 各状态订单（PENDING/PAID/COMPLETED/CANCELLED） | 脚本注入 |

---

## 执行记录

| 执行人 | 执行日期 | 测试环境 | 通过数 | 失败数 | 阻塞数 | 跳过数 |
|--------|---------|---------|--------|--------|--------|--------|
| QA Agent | YYYY-MM-DD | staging | - | - | - | - |
